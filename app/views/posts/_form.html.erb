<%= form_for(post) do |f| %>
  <% if post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :author %><br />
    <%= f.text_area :author %><br />
  </div>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_area :title %><br />
  </div>

  <div class="field">
    <%= f.label :summary %><br />
    <%= f.text_area :summary %><br />
  </div>
 
  <div class="field">
    <%= f.label :body %><br />
    <%= f.text_area :body %><br />
  </div>


  <div class="actions">
    <%= f.submit 'Click To Share', :class => 'btn btn-sm btn-danger' %>
  </div>
<% end %>
